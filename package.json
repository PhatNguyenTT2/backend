{
  "name": "backend",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "jest --verbose --runInBand",
    "test:watch": "jest --watch --verbose",
    "build:ui": "rm -rf dist && cd ../admin && npm run build && cp -r dist ../backend/dist",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "lint": "eslint .",
    "start:test": "cross-env NODE_ENV=test node --watch index.js",
    "migrate:location-capacity": "node migrations/add-location-max-capacity.js"
  },
  "dependencies": {
    "axios": "^1.12.2",
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongodb": "^6.20.0",
    "mongoose": "^8.18.3",
    "morgan": "^1.10.1",
    "node-cron": "^4.2.1",
    "qs": "^6.14.0",
    "vnpay": "^2.4.4"
  },
  "devDependencies": {
    "@eslint/js": "^9.22.0",
    "@stylistic/eslint-plugin-js": "^4.2.0",
    "@types/jest": "^30.0.0",
    "cross-env": "^7.0.3",
    "eslint": "^9.22.0",
    "globals": "^16.0.0",
    "jest": "^30.2.0",
    "mockingoose": "^2.16.2",
    "supertest": "^7.1.4"
  }
}